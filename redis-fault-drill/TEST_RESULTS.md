# 验证记录

验证日期：2026-09-07。

- Windows：Python 3.13，标准库单元测试通过，SSH 启动路径和状态/探测命令通过。
- 隔离环境：N150 上临时部署的 Redis 5.0.14，3 主 3 从，仅绑定 `127.0.0.1:18001–18006`。
- 测试结果：**9 项通过**（3 项单元测试、6 项实际集群集成测试），总耗时约 12.4 秒。

实际集群测试覆盖：

1. 注入后模拟丢失响应，确认部分执行失败会回退全部节点。
2. 全部读写阻塞、提前恢复，旧演练定时器不会解除新演练。
3. 全部读写超时，服务器定时器自动解除容器暂停。
4. 写入返回 NOREPLICAS、读取成功，提前恢复后写入成功；恢复非默认原参数（包括 max-lag=0）。
5. 拒绝写入模式由服务器定时器自动恢复。
6. 恢复定时器创建失败时，不注入故障。

另验证了重复端口拒绝、探测键路由，以及一个节点恢复失败时其余节点仍会被恢复。
测试结束后，六个临时容器、匿名数据卷、临时定时器及测试目录均已清理。

## 实际目标交付状态

- `192.168.2.126:7001–7006`：Redis **5.0.14**，3 主 3 从，cluster_state=ok。
- 所有容器 paused=False；min-replicas-to-write=0。
- 三个主节点的独立 GET/DEL 探测均成功，未注入实际业务集群故障。
- 原六个 `redis-7001` 至 `redis-7006` 停止容器的实际版本为 **7.2.16**，已继续保留。
- 本次切换前的 Redis 7 配置及数据备份位于服务器 `/opt/stacks/redis-cluster-redis7-backup-20260907-001552`。
- Redis 5 使用 `/opt/stacks/redis-cluster/7001..7006/data-redis5`；原 Redis 7 的 data 目录保留。

未执行服务器重启或业务客户端端到端压测；定时恢复测试通过真实 systemd 定时器执行。
